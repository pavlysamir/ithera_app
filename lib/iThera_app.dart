import 'package:flutter/material.dart';

class ItheraApp extends StatelessWidget {
  const ItheraApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'iThera',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
    );
  }
}
