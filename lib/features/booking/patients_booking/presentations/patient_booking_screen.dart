import 'package:flutter/material.dart';

class PatientBookingScreen extends StatelessWidget {
  const PatientBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Patient Booking'),
      ),
    );
  }
}
